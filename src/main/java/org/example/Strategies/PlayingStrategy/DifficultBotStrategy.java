package org.example.Strategies.PlayingStrategy;

import org.example.Models.Board;
import org.example.Models.Move;

public class DifficultBotStrategy implements BotStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
